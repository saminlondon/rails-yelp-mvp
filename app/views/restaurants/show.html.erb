<h2><%= @restaurant.name %></h2>
<p><%= @restaurant.address %></p>
<p><%= @restaurant.phone_number %></p>
<p><%= @restaurant.category %></p>


<%= link_to "back to list", restaurants_path, class: "btn btn-light" %>


<p><strong>Reviews</strong></p>
<ul class="list-group">
  <% @restaurant.reviews.each do |review| %>
    <li class="list-group-item"><%= review.content %></li>
  <% end %>
</ul>

<div class="container">
  <h1>Reviews#new</h1>

  <h1>New review</h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Submit review", class: "btn btn-light" %>
  <% end %>
</div>

<%# <%= link_to "Write a new review", new_restaurant_review_path(@restaurant), class: "btn btn-light" %>
<%# don't need this bits as you've embedded %>
